BWAP
====

**B**uzzing **W**ires **A**rg **P**arser

*Versatile runtime command line parser*

Building and Installation
-------------------------

Simply run `nimble install` to install or `nimble test` to run tests. There are no extra dependencies.

Also run `nimble build_examples` to build example programs, which will appear in the `examples` directory.
